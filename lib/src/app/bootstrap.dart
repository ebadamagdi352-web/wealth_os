import 'package:flutter/widgets.dart';
import 'package:wealth_os/src/app/app.dart';

/// Starts the Wealth OS application.
///
/// Task 004 scope: calls [runApp] and nothing else. Framework initialization,
/// dependency wiring, and error handling belong to later tasks.
void bootstrap() {
  runApp(const WealthOsApp());
}
